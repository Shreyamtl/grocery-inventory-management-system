apiVersion: v1
kind: Service
metadata:
  name: grocery-service
  namespace: grocery-app
  labels:
    app: grocery-inventory
spec:
  type: NodePort
  selector:
    app: grocery-inventory
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
